import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    display: "flex",
    width: "100%",
    position: "relative",
  },
  pagination: {
    position: "absolute",
    right: 0,
    top: 40,
  },
});
