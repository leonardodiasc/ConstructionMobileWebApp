import { StyleSheet } from "react-native";

export default StyleSheet.create({
  title: {
    fontFamily: "Jakarta Semibold",
    fontSize: 24,
    lineHeight: 40,
    textAlign: "center",
    color: "#000000",
  },
});
